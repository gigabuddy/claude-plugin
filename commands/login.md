---
description: Sign in to Gigabuddy — opens a one-time device code + URL to approve in your browser
---

Call the `login` tool now.

- If it reports already signed in: tell the user in one line who they're signed in as, and stop.
- Otherwise relay the verification URL and code it returns, clearly and prominently, and tell the user to open the URL and approve. Then wait for the tool's completion (it polls) and confirm sign-in in one line.
- If the tool is deferred out of your visible tool list, find it via tool search — never by searching the filesystem.

No other tools, no extra commentary.
