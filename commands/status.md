---
description: Show your Gigabuddy session status — signed-in identity, connected place, and presence
---

Call the `session_status` tool now.

Summarize the result for the user in 2-3 lines: signed in as whom (or not signed in — offer `/gigabuddy:login`), connected to which place as which identity (or not connected — mention they can ask you to connect), and anything notable (peers online, current activity).

No other tools, no extra commentary.
